<UserControl x:Class="SchoolBookingApp.MVVM.View.AddStudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodel="clr-namespace:SchoolBookingApp.MVVM.Viewmodel"
             xmlns:converters="clr-namespace:SchoolBookingApp.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodel:AddStudentViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanInverterConverter x:Key="BooleanInverterConverter"/>
        <converters:ValueTupleToRelationshipConverter x:Key="ValueTupleToRelationshipConverter"/>
    </UserControl.Resources>
    <Grid Background="#101010">
        <Viewbox Stretch="Uniform">
            <Grid Width="800"
              Height="auto">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="75"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="75"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2"
                       Grid.Row="0"
                       Text="{Binding AddStudentTitle}"
                       Style="{DynamicResource TitleText}"/>
                <TextBlock Grid.Column="0"
                       Grid.Row="1"
                       Text="{Binding SelectStudentLabel}"
                       Style="{DynamicResource SubtitleText}"/>
                <ComboBox Grid.Column="1"
                      Grid.Row="1"
                      SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
                      ItemsSource="{Binding AllStudents}"
                      Style="{DynamicResource SelectPersonComboBoxStyle}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} {1}">
                                        <Binding Path="FirstName"/>
                                        <Binding Path="LastName"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock Grid.Column="0"
                       Grid.Row="2"
                       Text="{Binding FirstNameLabel}"
                       Style="{DynamicResource SubtitleText}"/>
                <TextBox Grid.Column="1"
                     Grid.Row="2"
                     Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource DataFieldTextBoxStyle}"/>
                <TextBlock Grid.Column="0"
                       Grid.Row="3"
                       Text="{Binding LastNameLabel}"
                       Style="{DynamicResource SubtitleText}"/>
                <TextBox Grid.Column="1"
                     Grid.Row="3"
                     Text="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource DataFieldTextBoxStyle}"/>
                <TextBlock Grid.Column="0"
                       Grid.Row="4"
                       Text="{Binding ClassNameLabel}"
                       Style="{DynamicResource SubtitleText}"/>
                <TextBox Grid.Column="1"
                     Grid.Row="4"
                     Text="{Binding ClassName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource DataFieldTextBoxStyle}"/>
                <TextBlock Grid.Column="0"
                       Grid.Row="5"
                       Text="{Binding DateOfBirthLabel}"
                       Style="{DynamicResource SubtitleText}"/>
                <DatePicker Grid.Column="1"
                            Grid.Row="5"
                            VerticalAlignment="Top"
                            SelectedDate="{Binding DateOfBirth, Mode=TwoWay}"
                            Style="{DynamicResource DateTimePickerDatePickerStyle}"/>
                <TextBlock Grid.Column="0"
                           Grid.Row="6"
                           Text="{Binding ParentsLabel}"
                           Style="{DynamicResource SubtitleText}"/>
                <ListBox Grid.Column="1"
                         Grid.Row="6"
                         ItemsSource="{Binding Parents, Converter={StaticResource ValueTupleToRelationshipConverter}}"
                         Style="{DynamicResource ParentsListBox}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} {1} - {2}">
                                        <Binding Path="Person.FirstName"/>
                                        <Binding Path="Person.LastName"/>
                                        <Binding Path="RelationshipType"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2"
                           Grid.Row="7"
                           Text="{Binding StatusMessage}"
                           Style="{DynamicResource StatusMessageStyle}"/>
                <Grid Grid.Column="0" Grid.ColumnSpan="2"
                  Grid.Row="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                        Content="{Binding AddUpdateButtonLabel}"
                        Command="{Binding AddUpdateStudentCommand}"
                        Style="{DynamicResource StandardSelectionButtonStyle}"/>
                    <Button Grid.Column="1"
                        Content="{Binding ClearFormsButtonLabel}"
                        Command="{Binding ClearFormsCommand}"
                        Style="{DynamicResource StandardSelectionButtonStyle}"/>
                    <Button Grid.Column="2"
                        Content="{Binding DeleteButtonLabel}"
                        Command="{Binding DeleteStudentCommand}"
                        Style="{DynamicResource StandardSelectionButtonStyle}"
                            IsEnabled="{Binding IsNewStudent, Converter={StaticResource BooleanInverterConverter}}"/>
                </Grid>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
